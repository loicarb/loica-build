require 'spec_helper'

describe Loica::Build do
  it 'has a version number' do
    expect(Loica::Build::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
